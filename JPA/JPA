1. hibernate-ddl-auto
2. validations
3. exception response


how to implement paging for specific data
as i can see only findAll method for page



optional does not throw exception in case of empty list in optional box
or else throw does not work there


cusotm exception advangtages:

1. http status codes acocording provide
2. message customize

disadvantage:

1. boilerplate code, try catch each time for same exception write
for different enpoints

@ExceptionHndler

Advantage

no need to write try catch for each endpoint, just write one method to handle the exception for all endpoints

disadvantage
not accessible to other dontroler class



User-facing message → Something clean and generic like "failure: Internal Server Error".

Technical details → Logged internally (to console, file, or monitoring system) using a logger.

This way:

End users don’t see sensitive stack traces or database errors.

Developers still have full visibility in logs to debug the root cause.




When you return a Java object from a Spring Boot controller:

Spring uses Jackson to convert it into JSON.

Jackson needs getters to read field values.

Without getters, Jackson won’t know how to serialize your object into JSON.


========================
Bean Validations

dependency:
starter validation

Annotatiaons:
can provide cutom message in each of them

@NotNull
@Email
@Pattern
@Min
@Max
@NonBlank
@Pattern(regexp = "")

@Column(name = "email_id", nullable = false, unique = true)

===================

basic regex:
^->start of string
$->end of string
[abc]->any one of a,b,c
[^abc]->any character except a,b,c
[0-9]->any digit from 0 to 9
[a-zA-Z]->any letter from a to z or A to Z
\d->any digit (equivalent to [0-9])

+ : one or more occurrences of the preceding element
* : zero or more occurrences of the preceding element

1. regex for digits from 1-10

"^[0-9]+$"       ->any digit from 0 to 9, one or more times
"^[0-9]{10}$"    ->any digit from 0 to 9, exactly 10 times
"^[\\d{10}]$"    ->any digit from 0 to 9, exactly 10 times (using \d for digits)

2. regex for allow all charcters except a, b, c

"^[^abc]+$"     ->any character except a, b, c, one or more times